package com.big.tuwien.SmartMatcher.views.bubble;

/**
 * Observer interface of the BubbleView.
 * @author alex
 *
 */
public interface BubbleViewListener {
	
	/**
	 * This method is called to notify about an event which occurred at the BubbleView.
	 * @param event The type of event that happened.
	 * @param b The bubble associated with the event.
	 */
	public void bubbleViewEvent(int event, Bubble<?> b);
}
